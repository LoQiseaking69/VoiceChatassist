name: Build Voice-Activated Chat System Executable

on:
  push:
    branches:
      - main
    paths:
      - 'voice-activated-assistant/**'

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install fastapi uvicorn[standard] pydantic pyinstaller sqlalchemy asyncpg aiosqlite cryptography transformers spacy nltk SpeechRecognition pyttsx3

    - name: Install additional dependencies for advanced ML models
      run: |
        pip install huggingface_hub

    - name: Download NLP models
      run: |
        python -m spacy download en_core_web_trf
        python -m nltk.downloader punkt

    - name: Create Executable
      run: pyinstaller --onefile --clean --add-data "voice-activated-assistant/*.py;voice-activated-assistant" voice-activated-assistant/main.py

    - name: Upload Executable
      uses: actions/upload-artifact@v2
      with:
        name: voice-activated-chat-system-executable
        path: dist/*.exe